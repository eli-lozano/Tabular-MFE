import { createTheme } from "@mui/system";

const theme = createTheme({
    palette: {
        primary: {
            light: '#F8F0E5',
            main: '#EADBC8',
        },
        secondary: {
            main: '#DAC0A3',
            dark: '#0F2C59',
        },
    },
});

export default theme;
