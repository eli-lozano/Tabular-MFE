import { SxProps, Theme } from "@mui/system";

const classes: Record<string, SxProps<Theme>> = {
    container: {
        width: '99%',
        height: 'auto',
        display: 'flex',
        mt: 1.5,
        mb: 0.25,
        p: 1,
    },
};

export {
    classes
}